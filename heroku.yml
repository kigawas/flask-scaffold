build:
  docker:
    web: Dockerfile
run:
  web: poetry run gunicorn -b $IP:$PORT --access-logfile - --error-logfile - aioapp:aioapp -k aiohttp.worker.GunicornWebWorker

release:
  command:
    - echo $HOME
  image: web
